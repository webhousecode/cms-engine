services:
  blog:
    build: .
    ports:
      - "8081:8080"
    volumes:
      - ./content:/app/content:ro
      - ./public:/app/public:ro
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
