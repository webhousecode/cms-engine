# Revalidation secret — shared between CMS admin and this site.
# Generate with: openssl rand -hex 32
REVALIDATE_SECRET=

# Enable LiveRefresh in production (auto-enabled in dev)
# NEXT_PUBLIC_LIVE_REFRESH=true

# Site URL for sitemap generation
# NEXT_PUBLIC_SITE_URL=https://mysite.com
